<div class="ui card">
    <div class="content">
        <div class="description">
            <div class="ui statistics">
                <div class="value">
                    {{ total.result }}
                </div>
                <div class="label">
                    {{ theName | uppercase }}
                </div>
            </div>
        </div>
        <div class="meta">
            <span class="red">{{ total.given }}</span>
            <span class="green">{{ total.received }}</span>
        </div>

    </div>
</div>
